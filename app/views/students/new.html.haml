.container
  .row
    .col-md-3
    .col-md-6
      %h3 Schrijf je hieronder in voor een proefles.

      = render 'form'

      = link_to 'Back', students_path if current_user && (current_user.teacher? || current_user.admin? )
    .col-md-3
